<!DOCTYPE html>
<html>
<head>
  <title>FourFourForum</title>
  <link href='http://fonts.googleapis.com/css?family=Pathway+Gothic+One' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
    <body>
        <% flash.each do | name, message | %>
      <divclass="flash-message flash-message-<%=@djname%>">
        <%=message%>
      </div>
        <%end%>
    </div>
      <div class="container">
        <div class='header'>FourFourForum</div>
          <div class="navbar">
            <ul class="nav">
              <li><%= link_to "Home", root_path %></li>
              <li><%= link_to "Explore DJs", artists_path %></li>
              <li><%= link_to "Explore Mixes", mixes_path %></li>
              <% if  current_user %>
               <% if current_user.artist %>
              <li><%= link_to("My Profile", current_user.artist)  %></li>
              <%else%>
              <li><%= link_to "New DJ Profile", new_artist_path %></li>
              <%end%>
              <%end%>
              <li><%= link_to "Upload Mix", new_mix_path %></li>
              <% if current_user %>
              <li><%= link_to 'Logout', session_path(session), method: :delete, data: { confirm: 'Are you sure?'} %></li>
              <% else %>
              <li><%= link_to 'Please Login', login_path %></li>
              <% end %>
            </ul>
          </div>
            <%= yield %>
      </div>
    </body>
</html>
