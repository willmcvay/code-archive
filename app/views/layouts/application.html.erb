<!DOCTYPE html>
<html>
<head>
  <title>Cookin' On Rails</title>
  <link href=''http://fonts.googleapis.com/css?family=Fauna+One''>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
<div class="navbar navbar-fixed-top">

</div>
  <ul class="nav nav-tabs">
    <li class="active">
      <li><%= link_to "Home", root_path %></li>
      <li><%= link_to "Recipes", recipes_path %></li>
      <li><%= link_to "Ingredients", ingredients_path %></li>
      <li><%= link_to 'New Recipe', new_recipe_path %></li>
      <li><%= link_to 'Sign Up Here', new_user_path %></li>
      <li>Search Recipes<%= search_form_for @q, url: search_recipes_path, html: {method: :post} do |f| %><%= f.text_field :name_cont %><%= f.submit %>
        <% end %></li>
    </ul>
    <ul>
      <% if current_user %>
      <li>Hello, <%= current_user.email %></li>
      <li><%= link_to 'Logout', session_path(session), method: :delete, data: { confirm: 'Are you sure?'} %></li>
        <% else %>
      <li><%= link_to 'Please Login', login_path %></li>

        <% end %>
    </ul>
        <% flash.each do |name, message| %>
      <div class='flash-message flash-message-<%=name%>'>
        <%= message %>
        <% end %>
        <%= yield %>
      </div>

  </div>
</body>
</html>
